import {
  mainBg, mainButtonColor, mainTextColor, borderColor, subMainTextColor, buttonSubcolor, sliderColor, plateColor, subMainBg,
} from '../constants/colors-dark';

const themeDark = {
  mainBg,
  mainButtonColor,
  mainTextColor,
  borderColor,
  subMainTextColor,
  buttonSubcolor,
  sliderColor,
  plateColor,
  subMainBg,
};

export default themeDark;

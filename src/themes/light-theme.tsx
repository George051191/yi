import {
  mainBg,
  mainButtonColor,
  mainTextColor,
  borderColor,
  subMainTextColor,
  buttonSubcolor,
  sliderColor,
  plateColor,
  subMainBg,
  placeholderColor,
  tomatoColor,
  errorColor,
  focusColor,
  lightPeach,
  modalOverlay,
} from '../constants/color';

const themeLight = {
  mainBg,
  mainButtonColor,
  mainTextColor,
  borderColor,
  subMainTextColor,
  buttonSubcolor,
  sliderColor,
  plateColor,
  subMainBg,
  placeholderColor,
  tomatoColor,
  errorColor,
  focusColor,
  lightPeach,
  modalOverlay,
};

export default themeLight;
